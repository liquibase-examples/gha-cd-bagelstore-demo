diff --git a/app/src/models.py b/app/src/models.py
index ed7fbf2..9f7876a 100644
--- a/app/src/models.py
+++ b/app/src/models.py
@@ -14,6 +14,7 @@ class Product:
     name: str
     description: str
     price: float
+    rating: int = 0
 
     @staticmethod
     def from_db_row(row):
@@ -22,7 +23,8 @@ class Product:
             id=row[0],
             name=row[1],
             description=row[2],
-            price=float(row[3])
+            price=float(row[3]),
+            rating=int(row[4]) if len(row) > 4 else 0
         )
 
 
diff --git a/app/src/routes.py b/app/src/routes.py
index aeab7d9..cc5c56f 100644
--- a/app/src/routes.py
+++ b/app/src/routes.py
@@ -26,7 +26,7 @@ if not DEMO_USERNAME or not DEMO_PASSWORD:
 def index():
     """Homepage - product catalog"""
     products = execute_query(
-        'SELECT id, name, description, price FROM products ORDER BY name'
+        'SELECT id, name, description, price, rating FROM products ORDER BY name'
     )
 
     products_list = []
@@ -71,7 +71,7 @@ def cart():
 
     for item in cart_items:
         product_row = execute_one(
-            'SELECT id, name, description, price FROM products WHERE id = %s',
+            'SELECT id, name, description, price, rating FROM products WHERE id = %s',
             (item['product_id'],)
         )
         if product_row:
@@ -130,7 +130,7 @@ def checkout():
 
     for item in cart_items:
         product_row = execute_one(
-            'SELECT id, name, description, price FROM products WHERE id = %s',
+            'SELECT id, name, description, price, rating FROM products WHERE id = %s',
             (item['product_id'],)
         )
         if product_row:
diff --git a/app/src/static/css/style.css b/app/src/static/css/style.css
index b00f8a5..d0db2d2 100644
--- a/app/src/static/css/style.css
+++ b/app/src/static/css/style.css
@@ -94,6 +94,13 @@ h2 {
     margin-bottom: 0.5rem;
 }
 
+.product-card .rating {
+    color: #f39c12;
+    font-size: 1.2rem;
+    margin-bottom: 0.5rem;
+    letter-spacing: 2px;
+}
+
 .product-card .description {
     color: #7f8c8d;
     margin-bottom: 1rem;
diff --git a/app/src/templates/index.html b/app/src/templates/index.html
index 38f41ce..90ef1e2 100644
--- a/app/src/templates/index.html
+++ b/app/src/templates/index.html
@@ -10,6 +10,9 @@
         {% for product in products %}
         <div class="product-card">
             <h3>{{ product.name }}</h3>
+            <div class="rating">
+                {% for i in range(product.rating) %}★{% endfor %}{% for i in range(5 - product.rating) %}☆{% endfor %}
+            </div>
             <p class="description">{{ product.description }}</p>
             <p class="price">${{ "%.2f"|format(product.price) }}</p>
             <form action="{{ url_for('main.add_to_cart', product_id=product.id) }}" method="POST">
diff --git a/db/changelog/changelog-master.yaml b/db/changelog/changelog-master.yaml
index 93d17da..592fd17 100644
--- a/db/changelog/changelog-master.yaml
+++ b/db/changelog/changelog-master.yaml
@@ -46,6 +46,11 @@ databaseChangeLog:
       file: changesets/007-seed-inventory.sql
       relativeToChangelogFile: true
 
+  # Feature Enhancement
+  - include:
+      file: changesets/008-add-product-ratings.sql
+      relativeToChangelogFile: true
+
   # Version Tag
   - changeSet:
       id: tag-v1.0.0
